# -*-perl-*-
#  //brazil/src/shared/interface/RateEstimator/mainline perforce.amazon.com:9591/Config

package.RateEstimator = {
    interfaces = (1.0);
    deploy = {
        generic = true;
    };
    scope = interface;

    dependencies = {
        1.0 = {
        };
    };

    test-dependencies = {
        1.0 = {
            junit = 4.1;
        };
    };

    build-system = happytrails;
    build-tools = {
        1.0 = {
            JDK = 1.5;
            HappyTrails = 3.1;
            HappierTrails = 3.1;
        };
    };

    # For more information on how to declare targets for HappyTrails packages
    # see: https://w.amazon.com/?BrazilBuildSystem/HappyTrails/GettingStarted
    targets = {
        RateEstimator-1.0 = { type = javalibrary; };
    };
};
